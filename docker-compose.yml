version: '3.7'
services:
  ejabberd:
    image: ejabberd/ecs
    ports:
      - '5222:5222'
      - '5269:5269'
      - '5280:5280'

    # command: ['docker exec -it react-chat_ejabberd_1 bin/ejabberdctl register admin localhost 123456']

  # app:
  #   container_name: react-chat
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   volumes:
  #     - '.:/app'
  #     - '/app/node_modules'
  #   ports:
  #     - '3001:3000'
  #   depends_on:
  #     - 'ejabberd'
  #   environment:
  #     - NODE_ENV=development